# History Instruction

用途：归档已经完成或废弃的阶段合同、执行指令和重要规划结论。

本文件只存历史，不作为当前执行依据。当前执行依据见：

- `Work_Formulation.md`
- `Work_Instruction.md`
- `Work_Task_Prompts.md`

---

## 归档规则

- 每个阶段结束后，由主窗口确认可归档。
- 归档内容应包含阶段目标、允许/禁止范围、关键小工单、验收结论。
- 已归档内容不得在当前阶段直接修改；如需引用，只读查看。
- 旧架构改写阶段的完整历史已移入 `ReconstructionDolder/`。

---

## 当前归档索引

### 2026-06-01：右键上下文菜单阶段

阶段名称：功能补充 - 主界面 / 周界面右键上下文菜单。

归档结论：

- 右键上下文菜单阶段已完成并通过 CM-4 整体验收。
- 本阶段可归档。
- 后续可继续进入月界面功能补齐或新的功能补充规划。

阶段目标：

- 主界面日程空状态区域和列表空白区域支持页面级右键菜单。
- 周界面日期白色空白区域支持页面级右键菜单。
- 菜单采用公共 `ActionContextMenu` 组件。
- 已实装动作：
  - 添加
  - 日 / 周 / 月 / 待办视图切换
- 保持禁用或占位：
  - 换肤
  - 排序
  - 筛选
  - 四象限视图

关键小工单：

- `CM-0`：右键菜单现状审查与精确边界。
- `CM-1`：公共 icon-text 上下文菜单组件试点。
- `CM-2`：主界面日程区域右键菜单接入。
- `CM-3`：周界面日期空白区域右键菜单接入。
- `CM-4`：右键上下文菜单整体验收与归档准备。

关键提交：

- `8a6c551 docs: record cm-0 context menu boundary review`
- `6fb847f refactor: add reusable action context menu`
- `3481655 feat: add dashboard context menu bridge`
- `76f2f94 feat: add week empty area context menu`
- `bae834e docs: close context menu validation`

保留边界：

- 未实现真实换肤 UI。
- 未实现排序和筛选完整功能。
- 未实现四象限视图。
- 未修改数据库字段。
- 未改变 `ScheduleCard` / `WeekScheduleCard` 既有右键菜单。
- 未改变周界面左键选中、双击跳日视图和卡片拖拽排序。

---

### 2026-06-04：月界面功能补齐阶段

阶段名称：功能补充 - 月界面功能补齐。

归档结论：

- 月界面功能补齐阶段已完成并通过 M-7 整体验收。
- 本阶段可归档。
- 后续不应继续在本阶段追加零散功能，应进入下一阶段规划或单独开启 UI 适配阶段。

阶段目标：

- 月格显示日程状态摘要，不在小格内塞完整日程文本。
- 今天只用日期数字金色表示，不再默认整格高亮。
- 单击日期表示用户选中，选中态使用整格高亮。
- 双击 / activated 日期跳转对应日视图，并关闭所有月界面持久浮窗。
- hover 日期显示只读预览，移出后隐藏。
- 单击日期显示月界面外侧持久 panel。
- 添加按钮优先使用用户选中日期，过去日期不可添加。
- 月界面左侧添加表单补齐 time / alarm / list picker 承接和保存结构。
- 月界面右键菜单复用 `ActionContextMenu`，只实装“添加”和“视图”。

关键小工单：

- `M-0`：月界面现状审查与交互边界定位。
- `M-1`：月格状态 marker 与今天金色日期。
- `M-2`：月格单击选中与双击跳日视图。
- `M-3`：hover 只读预览弹窗。
- `M-4`：单击日期持久 panel 壳。
- `M-5`：添加按钮使用选中日期。
- `M-5b`：月界面添加表单能力只读审查与 picker 承接方案确认。
- `M-5c`：月界面添加表单 UI 壳补齐。
- `M-5d`：月界面 time picker 接入。
- `M-5e`：月界面 alarm / list picker 接入。
- `M-5f`：月界面 priority / repeat / 保存结构对齐。
- `M-5g`：月界面添加能力整体验收。
- `M-6`：月界面右键菜单接入。
- `M-7`：月界面功能补齐整体验收。

关键提交：

- `ba1a7f5 docs: plan month view feature phase`
- `1057220 docs: record m-0 month view boundary review`
- `0d3f375 feat: add month view schedule markers`
- `44bff9f feat: update month date click behavior`
- `9002bd7 feat: add month day hover preview`
- `de35fe7 feat: add month day persistent panel shell`
- `4726625 feat: use selected month date for add action`
- `20eb377 feat: add month add form shell`
- `c7db4fa feat: connect month add time picker`
- `af76634 feat: connect month add alarm and list pickers`
- `bd0c378 feat: align month add save fields`
- `166b2c3 feat: add month view context menu`
- `742b6be docs: close month view feature validation`

保留边界：

- 未实现四象限视图。
- 未实现真实换肤 UI。
- 未实现排序和筛选完整功能。
- 未修改数据库字段。
- 未进行像素级 UI 适配和布局重排。
- 未做真实写库自动化验收；保存结构通过 mock / monkeypatch 验证。
- 周视图右键切换仍不携带“跳到指定周”的上下文，保持既有主路由边界。

---

### 2026-06-24 至 2026-06-25：月日程持久 panel 详情与编辑路由阶段

阶段名称：月日程持久 panel 详情与编辑路由，代号 `MP`。

归档结论：

- `MP-0` 到 `MP-5` 已完成并通过阶段验收。
- 月日程 panel 的列表承载、共享详情打开、跨视图生命周期、动态编辑路由和保存后刷新均达到阶段目标。
- 本阶段可归档；后续月界面视觉细调不再继续追加到 `MP` 合同。

阶段目标：

- 将月界面单击日期产生的持久 panel 改造成可承载日程列表的紧凑界面。
- 日程项双击打开共享 `ScheduleDetailPop`，并维护父 panel 与子详情生命周期。
- 普通日 / 周 / 月 / 待办视图切换不销毁月 panel；显式关闭和应用退出继续清理。
- 详情编辑按当前可见视图路由，不把创建时 `source_view` 作为最终决策。
- 保存后刷新月格 marker、hover cache、已打开 panel、子详情和相关视图。

关键小工单：

- `MP-0`：现状审查与路由边界定位。
- `MP-1`：月日程 panel 紧凑列表 UI 与数据承载。
- `MP-2`：日程项双击打开共享详情弹窗。
- `MP-3`：panel 生命周期与跨视图保留规则。
- `MP-4`：详情编辑请求按当前可见视图动态路由。
- `MP-5`：保存后多视图刷新与阶段整体验收。

关键提交：

- `d40ed8d docs: plan month day panel detail routing stage`
- `c655633 docs: record mp-0 month panel routing review`
- `f75e3b4 refactor: compact month day panel list ui`
- `0887b1c feat: open schedule detail from month day panel`
- `527a021 fix: retain month day panels on view hide`
- `affecc7 docs: record mp-4 detail edit routing validation`
- `7fbd018 fix: refresh month panel detail changes`
- `1d4607e fix: retain month day panels across view switches`

保留边界：

- 未修改数据库字段、重复规则写入语义或 `db_manager` 对外 API。
- `ScheduleDetailPop` 不直接调用具体页面；`MainWindow` 继续承担路由和刷新协调。
- 未进行真实写库自动化验收；使用 fake schedule / monkeypatch / offscreen smoke 验证。
- `MainWindow` 完整 offscreen 进程曾在断言完成后以 code 1 收尾，刷新断言本身通过；真实桌面环境仍保留回归检查。

---

### 2026-06-29 至 2026-07-06：坐标看板与主题选色弹窗第一版

阶段名称：坐标看板与主题选色弹窗收尾。

归档结论：

- 坐标看板第一版核心功能已完成并接入“更多 -> 坐标看板”。
- 真实日程投影、显示设置、颜色覆盖、持久化、详情交互、刷新和窗口交互达到第一版使用标准。
- 主题选色弹窗完成单窗口化、主题化、几何对齐及自定义颜色槽持久化。
- 本阶段可归档；后续只按独立增强处理完成日程开关、驻留定时刷新、自动扩窗和高 DPI / 多屏回归。

阶段目标与完成范围：

- 使用以“现在”为原点的单条水平时间轴展示过去与未来日程。
- DDL / 单时间点使用竖线，时间段使用横条；按清单颜色、重要性和完成状态绘制。
- 支持线性与 `log(t+1)` 非均匀映射、未来 / 双向 / 过去及五档时间范围。
- 对范围外点日程执行筛除，对跨边界时间段执行裁剪；hover 仍显示原始完整时间。
- 支持重叠轨道避让、画布拖动、滚轮缩放、窗口拖动、四边 / 四角 resize、置顶和关闭。
- hover 显示只读信息和时间辅助线；单击打开共享详情并复用跨视图编辑与刷新链。
- 设置页支持数轴、字体、清单颜色与透明度并实时驱动真实画布。
- 使用独立 `QSettings` 保存方向、跨度、非均匀映射、轴体 / 字体外观和按 `category_id` 建立的清单覆盖。
- 选色弹窗采用单一非原生 Qt 顶级窗口，不使用 `QGraphicsProxyWidget` 嵌套 `QColorDialog`。

保留边界：

- 第一版不提供独立的“显示完成日程”开关，范围内已完成日程固定显示在轴下方。
- 第一版不在看板持续打开且数据无变化时按分钟重算“现在”；打开和数据变化时刷新。
- 第一版不按轨道数量自动扩大窗口；保留屏幕边界回退、最小尺寸和手动 resize。
- Windows 高 DPI、多屏边界、系统拾色和原生 resize 继续作为 UI 回归项，不阻塞阶段归档。
- 看板暂不展示待办和无有效时间的条目，不新增数据库字段。

---

### 2026-07-06：坐标看板归档后增强与最终回归

阶段名称：坐标看板第一版归档后增强收尾。

归档结论：

- 第一版归档时保留的完成日程开关、驻留刷新、轨道自动扩窗和高 DPI / 多屏回归均已完成。
- 设置页布局、未分类颜色、窗口拖动、resize 时间跨度和弹窗边界一并收口。
- 本阶段可归档；坐标看板不再保留当前活动工单，后续仅在出现真实缺陷时单独修复。

完成范围：

- 增加“已完成显示”开关，并与真实画布、效果预览、顶部说明和 `QSettings` 接线。
- 看板可见时每分钟重算相对时间；隐藏或关闭后停止定时器。
- 按当前可见轨道自动增高窗口，只增不缩，并受当前屏幕可用高度限制。
- 恢复设置齿轮旋转；设置页非交互区域及显示框外侧空白支持窗口拖动，最外 `12px` 保持 resize 优先。
- 最低高度设为 `320px`；清单颜色首列最多 4 项，第 5 项进入后续列。
- 清单颜色组增加“默认”项，控制无清单 / 未分类日程颜色与透明度并持久化。
- 外框 resize 不改变数轴两端时间跨度；只有显示框内滚轮主动缩放时间。
- 完成 Qt `1.0 / 1.5 / 2.0` 缩放、Windows DPR `1.25`、负坐标双屏和原生 resize 回退验证。

保留边界：

- 坐标看板仍不展示待办和无有效时间的条目。
- 不新增数据库字段，不把看板颜色覆盖写回清单表。
- 真实物理环境只有一块显示器；多屏使用负坐标双屏矩形覆盖边界算法，后续新增硬件环境时继续做人工回归。
- `schedule_axis_board.py` 仍是大型 UI 文件；未来扩展前先补测试再拆分，不在本阶段重构。

---

### 2026-07-08 至 2026-07-14：课程表模式阶段归档
阶段名称：日 / 周 / 月持久弹窗课程表模式阶段。
归档结论：
- 日界面课程表模式已完成真实日程渲染、颜色持久化、详情弹窗改色、完成 / 删除菜单、单击选中、双击详情、时间段拖拽 / 拉伸、DDL / 单时间线段拖拽、贴边滚动和拖拉刻度设置。
- 周界面课程表模式已完成 7 日并列渲染、每日独立滚动、真实日程块 / DDL 线段绘制、悬停预览、详情弹窗、右键菜单、选中态、时间段拖拽 / 拉伸、DDL 跨日拖拽和拖动自动翻周。
- 月界面持久日期弹窗在课表模式且当日有日程时已切换为只读课表框；无日程日期和卡片模式保持原样，并支持局部暗色切换。
- 模式选择、课表颜色、默认颜色和拖拉刻度均使用独立 `timetable_preferences` 持久化，不与坐标看板配置混用。
- 本阶段可归档；后续只在出现真实缺陷时单独修复，不继续追加拖拽、拉伸或暗色细修作为本阶段内容。
关键范围：
- `src/ui/dashboard.py`
- `src/ui/week_window.py`
- `src/ui/month_window.py`
- `src/ui/popups/month_day_panel.py`
- `src/ui/main_window.py`
- `src/ui/schedule_detail_pop.py`
- `src/utils/timetable_preferences.py`
- `assets/icons/drag.svg`
- `assets/icons/refresh.svg`
- `assets/icons/1.svg`
- `assets/icons/5.svg`
保留边界：
- 不把月格本体改为课表视图。
- 不在本阶段继续推进完整暗色模式；暗色模式已转入皮肤功能后的独立规划。
- 不实现搜索 / 筛选、导出、换肤、加密、云同步或自动化测试体系。

---

## 2026-07-14 至 2026-07-16：搜索筛选、排序、全局查询、导出 UI 与辅助看板收尾阶段归档

归档说明：

- 本段保存归档时的活动规划、执行合同和执行提示词原文。
- 实际完成范围已经超出早期“搜索 / 筛选第六轮”合同，后续连续完成排序、“全部日程”、导出 UI / Export-0~2、待办看板、坐标看板和月界面外观收尾。
- 当前真实状态以 `Final_Formulation.md` 和对应提交记录为准。

### 归档时 Work_Formulation.md

# Work Formulation

用途：记录当前阶段的总体规划、目标边界和小工单路线。由主窗口在审核决策窗口方案后维护。

---

# 当前状态：搜索 / 筛选功能第六轮

当前小工单：将搜索与筛选接入待办列表。

目标：

- 待办列表保留独立于日程的筛选基线和临时搜索状态。
- 搜索与筛选分别保存独立条件；搜索清空后恢复已应用筛选。
- 待办筛选只包含清单和重要性；待办搜索额外包含标题 / 标题加详情和模糊 / 精准匹配。
- 共享主窗口搜索框按日界面与待办界面分别保存和恢复关键词。
- 待办筛选存在实际约束时持续高亮工具栏筛选按钮，重置后取消高亮。

已归档阶段：

- 月界面功能补齐阶段。
- 月日程持久 panel 详情与编辑路由阶段（`MP-0 ~ MP-5`）。
- 坐标看板与主题选色弹窗第一版。
- 坐标看板归档后增强与高 DPI / 多屏回归。
- 日 / 周 / 月持久弹窗课程表模式阶段。
- 日界面卡片与课表搜索 / 筛选阶段。
- 周界面卡片与课表搜索 / 筛选阶段。
- 月界面当前自然月搜索 / 筛选阶段。

---

## 当前边界

- 本轮只接入主窗口待办列表，不接入独立待办看板窗口。
- 待办查询状态与日、周、月界面状态分离，不互相覆盖。
- 不增加完成状态和时间形式筛选，保留待办列表原有未完成 / 已完成排序结构。
- 不修改数据库结构，不保存搜索关键词和筛选条件到 `QSettings`。
- 不接入独立待办看板和“全部日程”全局结果页。

---

## 后续候选

1. 用户人工验收待办搜索、筛选、关键词恢复和筛选高亮。
2. 设计“全部日程”全局结果窗口，并复用查询选项控件。
3. 导出功能。
4. 自动化测试与 CI 基线。

### 归档时 Work_Instruction.md

# Work Instruction

用途：记录当前待执行的小工单阶段合同。执行窗口只能根据主窗口确认后的最终提示词行动。

---

# 当前状态：搜索 / 筛选第六轮小工单

小工单名称：待办列表搜索与筛选接入。

允许范围：

- `src/ui/todo.py`
- `src/ui/main_window.py`
- `src/ui/header.py`
- `src/ui/popups/day_query_options_panel.py`
- `manage_instruction/Work_Formulation.md`
- `manage_instruction/Work_Instruction.md`
- `manage_instruction/Work_Log.md`
- `manage_instruction/Work_Task_Prompts.md`

目标：

- 待办搜索与待办筛选使用独立弹窗和独立状态。
- 待办筛选提供清单和重要性；待办搜索额外提供搜索范围和匹配方式。
- 逐字符关键词和选项变化即时更新待办卡片；清空关键词后恢复待办筛选结果。
- 日界面与待办界面切换时分别恢复各自搜索关键词和筛选高亮。

---

## 当前禁止

- 不修改日、周、月界面既有查询条件和结果规则。
- 不接入独立待办看板窗口和“全部日程”全局结果页。
- 不修改数据库、Repository 和表结构。
- 不新增查询偏好持久化。
- 不增加待办状态、时间形式筛选，不修改待办拖拽、详情和右键规则。
- 不修改 `manage_instruction/ReconstructionDolder/` 历史归档区。

---

## 下一步

本轮完成后先人工验收待办搜索 / 筛选，再决定进入“全部日程”全局窗口。

### 归档时 Work_Task_Prompts.md

# Work Task Prompts

用途：保存当前由主窗口确认、可交给执行窗口直接执行的最终提示词。

---

# 当前状态：搜索 / 筛选第六轮执行提示词

本轮执行目标：

- 待办筛选只提供清单和重要性；待办搜索增加标题 / 标题加详情及模糊 / 精准匹配。
- 待办搜索初始条件继承已应用待办筛选，但搜索条件变化不回写筛选。
- 逐字符搜索即时更新待办卡片；清空关键词恢复待办筛选结果。
- 待办筛选选择“不限”或点击“重置”时即时恢复结果并取消筛选按钮高亮。
- 日界面和待办界面切换时分别恢复各自搜索文字、清空键和筛选高亮。
- 不修改数据库、独立待办看板和待办卡片既有交互规则。

验收方式：

1. 全量 Python 语法解析。
2. PyQt offscreen 检查待办精简弹窗、选项回填、实时恢复和动态清空键。
3. 主窗口离屏检查筛选继承、搜索隔离、日 / 待办关键词恢复和筛选高亮。
4. 人工 GUI 检查待办卡片结果、空结果提示和弹窗布局。

---

## 2026-07-16 至 2026-07-19：真实导出、日程交互与纪念日期阶段归档

阶段名称：Markdown / PDF / PNG 真实导出、日课表视觉与收放、月日期弹窗交互、纪念日期功能阶段。

归档结论：

- 导出链路已从 UI 骨架推进到可用闭环：Markdown、ReportLab PDF、固定画布分页 PNG 均可真实保存，内嵌和放大预览复用最终排版结果。
- PDF 使用 A4；PNG 支持 `2:3 / 16:9 / 3:4 / 1:1 / 9:16` 和自定义像素尺寸，多页输出进入同名文件夹并按 `_001 / _002…` 编号。
- 默认背景弹窗一次显示 8 项，包含 3 组程序化背景和 `assets/Output_Background/` 自动扫描的 21 张图片；英文文件名通过映射显示中文名。默认图片和用户自定义图片均复用按 PDF / PNG 目标比例裁剪、真实预览和最终导出链路。
- Markdown、PDF、PNG 已分别记忆各自背景、字体和尺寸状态；默认图片按稳定文件名恢复，自定义图片按本地路径恢复，裁剪状态按格式、来源和目标比例隔离。
- 日界面课表下半区已改为横贯窗口的主题遮罩布局，固定显示 7 个等高小时栏；卡片模式原边距不变。页面级右键菜单已增加仅限日界面的“收起 / 放下”，并保持头部坐标、20px 底部留白、原高度渐变、挂起兼容及跨视图自动恢复。
- 月界面日期弹窗已完成卡片 / 课表模式适配、置顶、空白区域右键菜单、课表日程块与时间线在当天范围内拖拉等交互收尾。
- 纪念日期功能已实装：独立竖形弹窗、置顶 / 关闭 / 添加入口、日期日历、提醒提前量、纪念日卡片、今天标题金色、过期排序、右键删除、空白区域多选菜单和主体空白拖动均已接入；本地状态由独立偏好模块维护。
- 日 / 周页面和纪念日期已出现“多选”菜单入口，但真实选择状态、选择计数、全选、批量完成 / 撤销完成 / 删除及危险操作确认尚未绑定，明确转入下一阶段。
- 背景资源审计确认当前共 21 张图片，Git 历史没有 `assets/Output_Background/` 删除记录；`GreenField.jpg` 映射为“绿茵场中”。
- 错误新建的根目录 `FINAL_VISION.md` 已删除，最终构想统一维护在 `manage_instruction/Final_Formulation.md`。

关键提交：

- `55db2c4 feat: enable markdown file export`
- `10ffa18 feat: 完成 ReportLab PDF 导出`
- `88b0278 feat: 增加真实 PDF 分页预览`
- `011eec1 feat: 接入默认导出背景选择`
- `5f02b05 feat: 完善 PNG 导出与预览打印`
- `4028757 feat: 重构日课表并增加右键收放`
- `69030bc feat: 完善课表弹窗与菜单交互`
- `bf899ed feat: 实装纪念日期与日程弹窗交互`
- `1b38018 feat: 完善导出背景配置与界面细节`
- `5b7e02a feat: 添加绿茵场背景并归档阶段成果`
- `e5e7428 docs: 修正最终构想归档位置`

保留边界：

- 多选当前只有入口壳，不得当作已完成功能；下一阶段必须先固定选择状态、跨视图边界、批量操作语义和危险操作确认。
- 在建立自动化测试与 CI 基线前，不进入 `每年` / 自定义重复规则的真实写入改造。
- 本地备份 / 恢复、字体与 Skin Preset、本地数据库加密和本地多账号保持独立阶段，不混入多选工单。
- 云同步继续采用本地优先路线，单独规划账号、设备、稳定 ID、删除墓碑、增量同步、离线队列、冲突解决、服务端和安全策略。
- 整体 UI 审美、设计变量、暗色模式和高 DPI 视觉统一放在云同步及其他功能完整实装之后集中处理。

### 归档时 Work_Formulation.md

# Work Formulation

用途：记录当前阶段的总体规划、目标边界和小工单路线。由主窗口在审核决策窗口方案后维护。

---

# 当前状态：日界面右键收起 / 放下已完成

本轮在日界面共享右键菜单增加窗口收放状态，卡片模式和课表模式共用同一实现；现有顶部挂起按钮、独立挂起窗口及周 / 月界面逻辑保持不变。

完成目标：

1. 日界面展开时右键菜单显示 `setup.svg + 收起`，收起后头部右键菜单在同一位置显示 `setdown.svg + 放下`。
2. 收起后保留完整 `HeaderBar` 以及搜索 / 按钮组到正文分界线之间的原留白；高度按搜索框和五个工具按钮的最大实际下沿加 `20px` 动态计算，不依赖固定窗口高度。正文隐藏但不销毁，底部两角沿用主窗口现有圆角。
3. 收起绘制继续使用收起前完整窗口高度作为渐变源，下沿颜色保持展开界面原坐标色；放下恢复收起前的窗口高度、尺寸约束、布局间距、卡片 / 课表模式和原本可见的详情弹窗，不额外刷新日程数据。
4. 收起状态切换周、月、待办或进入添加、排序、筛选等正文功能前自动放下，避免隐藏正文污染其他路由。
5. 收起状态仍可使用原挂起按钮；从挂起窗口恢复后继续保持收起状态，两套功能互不替代。
6. 页面级右键菜单不再显示禁用的排序 / 筛选行；收起 / 放下 SVG 保留约 `1px` 画布安全边距，避免外框裁切。

当前边界：

- 收放入口只在日界面启用，周 / 月 / 待办共享菜单不增加该动作。
- 不修改头部控件布局、天气行为、卡片 / 课表内部布局及数据刷新合同。
- 日期点击后的未来天气弹窗属于后续独立增强，本轮不提前实现。
- 项目 `.venv` 全量语法检查、日页面卡片 / 课表总体功能回归、右键与收放回归以及 PDF / PNG 共享面板回归均已通过，未发现功能被改写或破坏。

### 归档时 Work_Instruction.md

# Work Instruction

用途：记录当前待执行的小工单阶段合同。执行窗口只能根据主窗口确认后的最终提示词行动。

---

# 当前状态：日界面右键收起 / 放下已完成

允许修改：

- `src/ui/common/action_context_menu.py`
- `src/ui/header.py`
- `src/ui/dashboard.py`
- `src/ui/main_window.py`
- `assets/icons/setup.svg`
- `assets/icons/setdown.svg`
- 当前阶段管理文档

必须完成：

- 日界面右键菜单在展开 / 收起状态分别显示“收起 / 放下”和对应 SVG。
- 收起只隐藏正文，并按搜索框 / 工具按钮最大实际下沿加 `20px` 计算裁切高度；放下恢复原窗口高度和双模式状态。
- 收起后的主窗口渐变必须继续以展开前完整窗口高度采样，不得把上下沿颜色重新压缩到短窗口；收起状态进入挂起条时沿用同一渐变源高度。
- 收放动作不得触发刷新、重建卡片或重置课表位置。
- 收起状态进入正文功能或切换其他视图时必须先自动放下。
- 现有顶部挂起按钮必须保持原入口和原信号，收起状态挂起后仍可恢复为收起状态。
- 底部两角必须与原顶部圆角一致，且收起时禁止垂直缩放命中。

禁止范围：

- 不把收起功能替换为原挂起窗口。
- 不修改头部搜索、时间、天气和工具按钮布局。
- 不向周 / 月 / 待办菜单扩散收放动作。
- 不修改数据库、查询、排序和刷新生命周期。
- 右键菜单移除排序 / 筛选时不得删除头部工具栏对应按钮及其业务入口。

验收：

- 卡片 / 课表模式分别收起并放下后，模式和原窗口高度保持。
- 收起后窗口底边与搜索 / 工具按钮最大下沿相距恰好 `20px`；正文不可见，底部角点呈透明圆角。
- 收起前后 Header、搜索框及五个工具按钮的窗口纵坐标必须完全一致，不得由布局剩余空间引起整体下移。
- 收起后右键可点击“放下”；切换待办等其他视图时自动恢复正文。
- 原挂起按钮在展开和收起状态均可使用。
- 项目 `.venv` 语法检查、右键菜单回归和离屏收放集成测试通过。

### 归档时 Work_Task_Prompts.md

# Work Task Prompts

用途：保存当前由主窗口确认、可交给执行窗口直接执行的最终提示词。

---

# 当前任务：日界面右键收起 / 放下已完成

只在日界面共享右键菜单增加“收起 / 放下”：展开时使用 `setup.svg`，收起后同一位置使用 `setdown.svg`。收起仅隐藏日界面正文，并按搜索框与工具按钮最大实际下沿加 `20px` 计算窗口裁切高度，保留时间、日期、天气、搜索、工具按钮和窗口控制；短窗口继续按展开前完整高度采样主题渐变，使下沿保持原纵坐标颜色。放下恢复原高度、卡片 / 课表模式和可见详情弹窗，不触发数据刷新。正文相关操作和跨视图切换前自动放下。现有顶部挂起按钮与独立挂起窗口必须保持，收起状态挂起并恢复后仍为收起状态且渐变不跳色。周 / 月 / 待办菜单和内部逻辑不变。

执行状态：生产修改集中在共享右键菜单、头部 / 日面板菜单构造和主窗口收放状态。Header 布局项已固定为顶部对齐，避免正文隐藏后被 Qt 自动居中；项目 `.venv` 全量语法检查通过。提交前总体回归确认卡片 / 课表、搜索、搜索设置、筛选、排序、添加、视图选择、日期切换、课表时间映射与刷新、右键二级菜单、拖拉选项、严格 `20px` 底部留白、底部圆角、渐变原坐标采样、挂起兼容、跨视图自动放下及 PDF / PNG 共享面板均正常。页面级右键菜单已移除禁用的排序 / 筛选两行，但头部按钮功能不变；收起 / 放下 SVG 已增加约 `1px` 安全边距并通过像素断言。
