# Work Task Prompts

用途：保存当前由主窗口确认、可交给执行窗口直接执行的最终提示词。

---

# 当前任务：ReportLab PDF 真实导出与分页预览已完成

新增独立 ReportLab PDF 排版器，在不修改数据库、Repository 和统一 `ExportPayload` 字段的前提下，为 `ScheduleExportService` 增加真实 `.pdf` 写入入口；导出弹窗选择 PDF 后使用系统保存对话框并规范化后缀，内容类型和日 / 周 / 月 / 全部范围继续复用 `ExportOptions`，纯色 / 渐变背景、标题 / 详情 / 备注字体与颜色、加粗和斜体必须真实进入文件。中文字体需稳定显示并支持搜索复制，普通卡片整卡换页，只有单卡超过整页时才允许带“续”标记跨页。Markdown 行为保持不变，PNG 继续提示尚未接入。完成后验证 8 种字体、空结果、超长详情、真实弹窗分发、文本提取、逐页渲染、全量语法和 `git diff --check`，并更新依赖、工作日志和最终构想。

追加完成真实分页预览：设置变化使用 `300ms` 防抖，在后台单线程池中调用同一 ReportLab 排版器生成临时 PDF，只加载最新结果并清理旧文件；内嵌框显示第一页缩略图和总页数，放大弹窗使用 `QPdfDocument / QPdfView` 多页模式显示真实 A4 页框、分页间距、当前页 / 总页数和最终字体排版，不再维护 HTML 近似 PDF 布局。

实装默认背景选择：保留可移动横向弹窗、主题渐变窗口、头部以下半透明白色遮罩、贴合右上角的关闭键、一次显示 5 张卡片及鼠标滚轮双向循环；将 8 组程序化图案抽为共享预设，并从 `assets/Output_Background` 增加 GreenLeaf 图片作为第 9 项。默认使用第 1 组，点击卡片后更新默认缩略框并接入 ReportLab PDF 背景；GreenLeaf 在 A4 页面中居中等比铺满。PDF / PNG 模式中当前背景框使用白色选中边，Markdown 模式四个背景框均恢复原灰色边。本工单不接入 PNG 保存、PNG 图片裁剪和选择持久化。

实装 PNG 固定画布分页多图：参数化现有 ReportLab 页面尺寸，保持 PDF 默认 A4 不变；新增 `PngCanvasSpec`、比例预设实际像素、自定义尺寸校验、共享视觉样式、逐页 Qt PDF 栅格化、单页 / 多页命名、准确张数提示、覆盖确认和真实多页预览。GreenLeaf 首版按目标比例居中等比铺满，手动裁剪与选择持久化不在本工单。

执行状态：PNG 固定画布分页多图已完成并通过服务层、真实面板和既有 PDF 离屏回归。下一轮提示词应针对 GreenLeaf 手动裁剪焦点或背景选择持久化单独生成。

追加导出与主题细节收尾：将 PNG `3:2 / 16:9` 预设交换为竖向像素并保证预览宽高同步；内嵌预览外层固定为白色；在不改变放大预览布局的前提下绑定现有打印图标，PDF / PNG 逐页打印真实分页文档，Markdown 打印当前文本预览；共享详情弹窗的重要性 / 重复规则菜单选中背景和文字改为主题色。日界面右键模式 / 视图二级菜单只做交互复核，不在本工单修改；当前复核未复现失败。
