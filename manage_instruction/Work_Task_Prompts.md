# Work Task Prompts

用途：保存当前由主窗口确认、可交给执行窗口直接执行的最终提示词。

---

# 当前任务：整卡滚动与日 / 周卡片多选已完成

仅修改日界面和周界面卡片模式：鼠标滚轮每格移动一条完整卡片，日界面步长为 `60px` 卡片加 `8px` 间隙，周界面步长为 `46px` 卡片加 `2px` 间隙。滚动后新首卡必须准确对齐原首卡位置；根据窗口可视高度计算完整卡片数，并把不足一个步长的底部余量留空，禁止显示半张底部卡片。窗口 resize 和数据刷新后重算吸附范围。不得修改课表模式、卡片尺寸、既有间隙、拖拽排序、数据库或其他界面。

执行结果：共享整卡滚动组件已接入日、周卡片模式，定向语法检查和 PyQt6 离屏几何回归通过。

追补：日界面头部使用独立右键菜单，现已在日视图卡片模式补充“多选”；课表模式和非日视图不显示。

日卡片多选已实装：进入后打开独立经典弹窗，不占用或移动日程滚动区；日程圆点改为白色 / 主题上沿色 `65%` 透明度的选择圆。弹窗一行提供全选 / 全不选、完成、撤销、删除和退出图标按钮，按选择与状态动态启用。批量写入使用 Repository 原子方法；关闭弹窗、跨模式或跨视图自动退出。

追补：多选操作卡按钮外空白支持双击退出；多选中的日程卡片恢复拖拽。日界面拖拽生命周期现在会拦截 `QDrag.exec()` 嵌套事件循环内触发的 30 秒自动刷新和查询重绘，待拖拽结束后合并补刷新，避免刷新销毁活动拖拽源导致黏鼠标和 Qt 原生崩溃。离屏生命周期回归通过。

周卡片多选已实装：右键进入后打开独立经典双行弹窗并恢复周头部原空白。第一行用 `Mon` 至 `Sun` 和 `All` 圆形文本按钮选择范围，选中显示 `2px` 白边；第二行复用日弹窗的全选 / 全不选、完成、撤销、删除和退出图标按钮。周卡片圆点改为选择圆，批量操作复用原子接口，多选期间保留同列 / 跨日期拖拽，切换课表、离开周视图或进入添加页自动退出。

当前日 / 周多选弹窗统一使用主题上下沿渐变、头部以下 `70%` 白色遮罩和紧贴右上角的置顶 / 关闭键；置顶默认继承主窗口但可独立切换。共享弹窗、日界面和真实周窗口离屏视觉与交互回归通过。
