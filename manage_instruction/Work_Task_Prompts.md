# Work Task Prompts

用途：保存主窗口审核后的最终执行提示词或当前复核锚点。执行窗口只应执行本文件中的最终版本。

---

# 当前复核锚点：M-5 后续规划

## 状态

- `M-5（月界面添加按钮日期来源联动）` 已完成并提交。
- 月界面右键菜单 `M-6` 暂缓。
- 下一步候选：`M-5b（月界面添加表单能力只读审查与 picker 承接方案确认）`。

## 关键结论

- 月界面添加表单当前仍是极简壳，不能直接进入右键菜单接入。
- `M-5b` 必须先只读审查 picker 承接方式、时间语义、提醒/清单规则和重复规则真实值体系。
- `M-5c` 只做 UI 壳时，按钮可禁用或显示轻量 toast，但不得写入状态，不得打开 picker。
- `M-6` 右键菜单的“添加”应复用补齐后的月界面添加入口。

## 下一步

等待决策/顾问窗口产出 `M-5b` 最终执行提示词。
