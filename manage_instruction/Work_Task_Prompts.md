# Work Task Prompts

用途：保存当前由主窗口确认、可交给执行窗口直接执行的最终提示词。

---

# 当前状态：暂无待执行提示词

旧 `MP-5` 提示词已随 `MP-0 ~ MP-5` 阶段归档，不得继续执行。

下一阶段必须先完成：

1. 读取 `Final_Formulation.md` 和当前源码状态。
2. 在 `Work_Formulation.md` 写入阶段规划。
3. 在 `Work_Instruction.md` 写入阶段合同与小工单边界。
4. 由主窗口生成并确认单个小工单最终提示词。

在此之前，执行窗口不得自行启动源码修改。
