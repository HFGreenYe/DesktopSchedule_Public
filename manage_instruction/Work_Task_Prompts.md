# Work Task Prompts

用途：保存当前由主窗口确认、可交给执行窗口直接执行的最终提示词。

---

# 当前任务：ReportLab PDF 真实导出与分页预览已完成

新增独立 ReportLab PDF 排版器，在不修改数据库、Repository 和统一 `ExportPayload` 字段的前提下，为 `ScheduleExportService` 增加真实 `.pdf` 写入入口；导出弹窗选择 PDF 后使用系统保存对话框并规范化后缀，内容类型和日 / 周 / 月 / 全部范围继续复用 `ExportOptions`，纯色 / 渐变背景、标题 / 详情 / 备注字体与颜色、加粗和斜体必须真实进入文件。中文字体需稳定显示并支持搜索复制，普通卡片整卡换页，只有单卡超过整页时才允许带“续”标记跨页。Markdown 行为保持不变，PNG 继续提示尚未接入。完成后验证 8 种字体、空结果、超长详情、真实弹窗分发、文本提取、逐页渲染、全量语法和 `git diff --check`，并更新依赖、工作日志和最终构想。

追加完成真实分页预览：设置变化使用 `300ms` 防抖，在后台单线程池中调用同一 ReportLab 排版器生成临时 PDF，只加载最新结果并清理旧文件；内嵌框显示第一页缩略图和总页数，放大弹窗使用 `QPdfDocument / QPdfView` 多页模式显示真实 A4 页框、分页间距、当前页 / 总页数和最终字体排版，不再维护 HTML 近似 PDF 布局。

执行状态：已完成。下一轮提示词应针对 PNG 固定画布分页多图单独生成。
