# Work Instruction

用途：记录当前待执行的小工单阶段合同。执行窗口只能根据主窗口确认后的最终提示词行动。

---

# 当前状态：整卡滚动与日 / 周卡片多选已完成

允许修改：

- `src/ui/common/card_step_scroll_area.py`
- `src/ui/dashboard.py`
- `src/ui/week_window.py`
- `src/ui/header.py`
- `src/ui/main_window.py`
- `src/ui/common/action_context_menu.py`
- `src/ui/popups/schedule_multi_select_popup.py`
- `src/repositories/schedule_repository.py`
- `src/data/database.py`
- 当前阶段管理文档

必须完成：

- 日界面按 `68px` 整卡步长滚动。
- 周界面每列按 `48px` 整卡步长滚动。
- 滚动位置只能落在整卡吸附点。
- 内容超过可视区时，底部余量不得显示下一张残缺卡片。
- 窗口尺寸变化和数据刷新后重新计算可完整显示的卡片数与吸附范围。

禁止范围：

- 不修改卡片本身尺寸和现有间隙。
- 不修改日 / 周课表模式。
- 整卡滚动不修改拖拽排序、数据库、查询、筛选和刷新语义。
- 不扩散到月界面、待办看板或纪念日期。
- 日卡片多选不得扩散到日课表、周界面、月界面、待办或纪念日期。
- 批量操作只更新当前选中的实际卡片；不修改重复日程组和未来实例。
- 活动 `QDrag` 期间不得删除或重建拖拽源卡片；刷新请求必须延迟到拖拽结束后合并执行。
- 多选弹窗不得占用日 / 周卡片列表或头部布局，也不得参与日程分组、排序权重或数据库写回；多选中的日程卡片允许继续拖拽。
- 周多选只作用于周卡片模式，不扩散到周课表、日 / 月界面、待办或纪念日期。
- 周日期范围只限定全选 / 全不选的作用范围；完成、撤销和删除只处理当前实际选中的卡片 ID。

完成验收：

- 日界面 10 张卡片、`350px` 滚动区下，可视区收敛为 `332px`，单步为 `68px`，末端滚动值为 `340px`。
- 周界面 10 张卡片、`250px` 滚动区下，可视区收敛为 `238px`，单步为 `48px`，末端滚动值为 `240px`。
- 两种场景均验证滚动末端首卡上沿为 `0`，末卡下沿与 viewport 下沿完全重合。
- 日 / 周多选弹窗均使用主题渐变与头部以下 `70%` 白色遮罩；置顶默认继承主窗口但仅作用于弹窗自身。
- 未选中圆点为白色，选中圆点使用主题上沿色 `65%` 透明度；全选、完成、撤销和删除启用矩阵通过。
- 日弹窗为一行五个方形图标按钮；周弹窗第一行为八个圆形英文缩写文本按钮，第二行为同款五个功能图标按钮。
- 关闭弹窗和退出图标必须同步结束多选；拖拽中触发自动刷新时源卡片不得被销毁，结束后完成一次补刷新。
- 周弹窗两行首尾按钮中心误差不超过 `1px`；周头部恢复原始空白，不再承载多选菜单。
- 周一至周日与“全部”双向联动；全选 / 全不选图标、完成 / 撤销 / 删除灰化矩阵及批量调用通过。
- Repository 临时数据库批量完成与批量删除事务通过。
