# Work Log

用途：记录当前阶段执行过程、验证结果、风险和结论。

---

# 当前状态：暂无活动阶段日志

截至 2026-07-06 的功能补充、月日程持久 panel 路由、坐标看板与主题选色弹窗第一版记录已归档。

归档位置：

- `manage_instruction/History_Instruction.md`
- `manage_instruction/History_Log.md`

归档结论：

- `MP-0 ~ MP-5` 已完成并归档。
- 坐标看板与主题选色弹窗第一版已完成并归档。
- 下一阶段尚未规划，本文件等待新的执行记录。

---

## 新阶段记录规则

- 开工前记录 `git status --short --branch` 和既有 diff。
- 每轮记录实际修改文件、验证命令与结果、未完成事项和风险。
- 阶段完成后由主窗口复核并迁入 `History_Log.md`。

---

## 2026-07-06 坐标看板归档后增强：已完成显示开关

- 按用户确认的布局方案，在颜色设置区顶部增加与“非均匀显示”同高、同样式的“已完成显示”行及切换开关。
- 将“数轴”标题下移到与左侧“范围”标题同一水平高度；数轴组和清单组继续使用统一标题间距与组间距。
- 扩展清单颜色网格布局算法：第一清单列仍从“清单”标题下方开始，第二个颜色列及以后从“轴体”行开始，避免清单项顶到设置页上沿。
- 新开关默认开启；关闭后，设置效果预览和真实数轴画布同时过滤 `is_completed` 日程，范围、跨边界裁剪、未完成日程和详情交互保持不变。
- 顶部说明在关闭开关后显示“已完成：隐藏”；重新开启后恢复“线下：已完成”。
- `axis_board_preferences.py` 将 `show_completed` 纳入状态校验和 `QSettings` JSON，旧配置缺少该字段时默认开启，保持兼容。
- `git diff --check` 通过；因项目 `.venv` 的沙箱外执行仍受 Codex 工具额度审批限制，本轮未重新声明 PyQt offscreen 几何与运行态验收通过，需实机复核标题、开关和多列清单的视觉位置。
