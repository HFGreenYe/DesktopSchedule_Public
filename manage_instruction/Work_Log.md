# Work Log

用途：记录当前活动小工单的执行过程、验证结果和待复核事项。

---

# 当前状态：ReportLab PDF 真实导出已完成

## 2026-07-16：方案审查

- 已确认 `ScheduleExportService` 具备统一 payload、Markdown 渲染和 `write_markdown(file_path, options)` UTF-8 写入入口。
- 当前缺口仅为导出按钮的格式分发、系统保存路径、`.md` 后缀规范化及成功 / 失败反馈。
- 决定不修改数据库、Repository、导出 payload 和 Markdown 结构；PDF / PNG 留在后续独立小工单。

## 2026-07-16：实现与验证

- `ExportSchedulePanel` 已将既有 `export_requested` 信号接入真实格式分发。
- Markdown 使用系统保存文件对话框；默认文件名按内容类型和日 / 周 / 月 / 全部范围生成，用户输入其他或缺失后缀时统一改为 `.md`。
- 文件写入复用 `ScheduleExportService.write_markdown()`；取消保存静默返回，成功显示文件名，异常使用错误对话框展示原因。
- PDF / PNG 当前只显示“尚未接入”提示，不生成占位文件。
- 验证结果：85 个 Python 文件 AST 解析通过，语法错误为 0；使用纯内存 repository 和临时目录完成 UTF-8 Markdown 真实写入，中文标题和详情读取正确；`git diff --check` 通过。
- 复核确认项目 `.venv` 正常可用，解释器为 Python 3.11.9，PyQt6 可正常导入；此前 `Unable to create process` 来自沙盒内无法访问虚拟环境依赖的用户目录基础解释器路径，并非虚拟环境失效。
- 使用项目 `.venv` 和 `QT_QPA_PLATFORM=offscreen` 实例化真实 `ExportSchedulePanel`：模拟选择 `.txt` 路径后正确生成 `.md`，UTF-8 文件以 `# 日程导出` 开头；取消保存不落盘；选择 PDF 时只显示“PDF 导出尚未接入”，三项烟测均通过。

## 2026-07-16：PDF 技术方案对照

- 先在 `.codex_tmp` 完成 Qt `QPdfWriter` 与 ReportLab 两条隔离样本：两者均可生成中文 A4 多页文件，但 Qt 输出的中文复制 / 搜索出现乱码，ReportLab 的中文文本提取、首末条定位和视觉渲染均通过。
- 决定正式 PDF 使用 `reportlab==4.4.9`，不复用 Qt PDF 输出；PDF 排版独立放入 service 层，UI 只收集选项、保存路径和结果反馈。
- 方案确认普通卡片整卡换页；单卡本身超过整页时才拆成续卡。初版曾为页眉、分组、卡片和页脚增加半透明白色承载层，后续按预览一致性要求移除，避免覆盖用户背景并使白色备注文字消失。

## 2026-07-16：PDF 实现与验证

- 新增 `PdfTextStyle / PdfExportStyle` 和 `SchedulePdfExporter`，`ScheduleExportService.write_pdf()` 继续复用统一 payload、内容类型及日 / 周 / 月 / 全部范围过滤。
- 导出弹窗已将纯色 / 渐变、上下沿颜色、标题 / 详情 / 备注字体与颜色、加粗和斜体传入 PDF；PDF 固定 A4，尺寸控件继续只对 PNG 启用。
- PDF 保存对话框、`.pdf` 后缀规范化、取消保存、成功提示和失败对话框已接入；Markdown 分发与 `.md` 落盘回归通过，PNG 仍只提示尚未接入。
- 字体验证覆盖微软雅黑、宋体、黑体、仿宋、Times New Roman、Arial、Calibri 和 Georgia；英文字体遇中文时回退到微软雅黑，8 种组合在加粗 / 斜体下均可显示并提取中文和英文。
- 边界验证覆盖空结果、32 条 8 页普通多页、500 次重复详情的 5 页续卡、纯色角点颜色和渐变背景；文本提取包含页眉、范围、首条、末条、中文详情和英文混排，普通多页无非必要续卡。
- 使用项目 `.venv` 离屏实例化真实 `ExportSchedulePanel`，验证 PDF / Markdown 错误后缀纠正、取消保存、当前周与内容类型映射、三类字体 / 颜色、背景和强调选项；生成文件逐页渲染检查通过。
- 验证结果：按 `utf-8-sig` 读取的全量 Python AST 检查通过；`reportlab==4.4.9` 已安装到项目 `.venv`；`git diff --check` 通过。
- 预览一致性修正：PDF 不再强制绘制白色页眉、分组、卡片和页脚底板；以黑色标题、蓝色详情、白色备注和蓝色渐变背景完成弹窗映射断言、PDF 文本提取及逐页渲染复核，白色备注可见且无大面积白色卡片。

## 2026-07-16：PDF 无边框与预览交互修正

- 按导出实样反馈移除日程 / 待办卡片的半透明标题色轮廓，卡片仅保留排版留白；日期分组下划线继续用于区分日期段。
- PDF 放大预览正文不再使用 `70%` 透明选色叠加主题渐变，改为直接绘制当前纯色或上下沿渐变；Markdown 预览仍保持白色阅读背景。
- 放大预览头部和正文无文字区域已支持拖动，正文文字命中区域继续交给 `QTextEdit`，不牺牲选择和滚动行为。
- 离屏 Qt 验证覆盖渐变色样式、头部拖动和正文空白拖动；ReportLab 实样重新渲染确认卡片无框，8 种字体、空结果和 5 页超长续卡回归通过。
- 当时暂不实现真实 PDF 分页预览。项目 `.venv` 已确认包含 `QPdfDocument / QPdfView`；推荐后续由同一 ReportLab 排版器生成临时 PDF，经防抖与后台串行任务加载到多页 `QPdfView`，从源头保证预览与最终文件一致，避免继续维护第二套 HTML 近似排版。该方案已于 2026-07-17 按后文记录完成。

## 2026-07-16：导出范围弹窗视觉统一

- 放大预览关闭按钮改为绝对定位在窗口右上角，窗口比例变化后仍保持顶边和右边贴合；原头部及正文空白拖动逻辑不变。
- `CalendarPop` 新增默认关闭的导出专用主题参数，仅导出面板的“某日”入口启用：窗口使用主题上下沿渐变，日期区使用等效 `70%` 白色遮罩渐变，头部双击切换白色 / 灰黑色主题被禁用。
- 普通 `CalendarPop` 构造和原生 `paintCell` 路径保持不变，日界面仍保留既有深浅主题和头部双击切换；导出主题只在专用日期单元格绘制分支中接管背景和文字颜色。
- 某周 / 某月选择弹窗使用主题渐变窗口，头部下方正文遮罩统一为 `70%` 白色并保留 `2px` 白边；追补纠正初版仍保留内嵌卡片留白的问题，当前遮罩从头部下沿直接贴满左右和底部，白边沿整块正文层外沿绘制。
- 周 / 月弹窗增加非控件区域拖动：头部、标题和正文遮罩空白处可移动窗口，年份 / 周次 / 月份选择框、确定键和关闭键保持原交互，不作为拖动热区。
- 周 / 月关闭键改为绝对定位并紧贴右上角；正文遮罩不再依赖 QSS 单独圆角，改为自绘直角上沿与圆润下沿的填充 / 白边路径，修复右下角看起来发直的问题。
- 离屏验证分别覆盖 `week / month` 两种模式的拖动位移、控件排除、关闭键几何和右下角透明像素，周、月实图均通过。
- 追补确认截图中仍可见的方角来自 Windows 为 `Popup` 自动添加的矩形投影，而非正文遮罩路径；周 / 月窗口增加 `NoDropShadowWindowHint`，移除会暴露方形右下角的原生阴影，保留应用自身圆角和白边。
- 导出面板预览框不再随设置区剩余高度向下拉伸，固定高度与左侧“导出设置标题 + 三组设置区”自然高度一致，使其下沿与 PNG 文本和三段蓝线下端对齐。
- 默认预览尺寸改为 A4 纵向 `210:297` 比例；当前固定高度为 `298px`、默认宽度按比例计算为 `211px`，PNG 模式仍可按既有尺寸选项调整宽度。
- 离屏几何验证确认预览框与左侧设置区下沿差值不超过 `1px`，默认宽高比误差小于 `0.005`，周 / 月窗口均带禁用原生阴影标志。
- 项目 `.venv` 离屏验证覆盖关闭按钮贴边、默认日历主题切换保留、导出日历主题切换禁用、实际导出入口参数隔离和周 / 月遮罩白边；三类弹窗截图复核通过。

## 2026-07-16：导出设置布局收紧与字体逐项强调

- 导出面板固定为 `360px` 高，设置区上下内边距均为 `10px`；预览框下沿到窗口下沿的距离与“导出设置”上沿到头部下分界线的距离一致，裁掉原底部多余留白。
- 第一列内容 / 范围 / 格式与第二列背景 / 字体 / 尺寸之间增加独立 `10px` 间隔列，避免两组设置视觉粘连；日程范围四项字体统一为与内容类型选项相同的 `10px`。
- 字体区改为五行对齐矩阵：字体标题对齐“日程范围”，加粗 / 斜体列名对齐“某日”，标题 / 详情 / 备注依次对齐“某周 / 某月 / 全部”。每行按“字段名、中英切换、窄字体框、色块、加粗圈、斜体圈”排列。
- 加粗和斜体从整份 PDF 的共用开关改为标题 / 详情 / 备注各自独立状态，默认仅标题加粗；内嵌预览和 ReportLab 导出均读取同一组逐字段设置。
- 项目 `.venv` 离屏验证覆盖面板高度、上下间距、A4 比例、两列间隔、行对齐、字体框宽度和默认强调状态；PDF 边界、生产样例及实图渲染复核通过。

## 2026-07-17：真实 PDF 分页预览

- 日程范围四项按钮宽度从 `82px` 统一为 `76px`，内容类型、日程范围和导出格式的单选圈中心均为同一竖线；背景方框、字体区和标题 / 详情 / 备注左端继续固定在另一条竖线，不随范围按钮左移。
- 新增独立 `SchedulePdfPreviewController`：设置变化后 `300ms` 防抖，专用线程池限制为单线程串行生成；请求编号只允许最新结果进入 UI，过期任务文件自动删除。
- 预览与真实保存继续调用同一 ReportLab 排版器，并通过全局重入锁串行写入，避免后台预览和手动保存同时注册字体。
- 内嵌预览不再显示 HTML 近似结构，改为 `QPdfDocument.render()` 生成真实第一页缩略图并显示总页数；放大预览头部以下外层底板使用主题渐变叠加 `70%` 白色遮罩，内部中性灰 PDF 页框和尺寸保持不变。放大预览使用 `QPdfView.MultiPage + FitToWidth` 显示真实 A4 页框、分页间距、最终字体排版和当前页 / 总页数，滚动条隐藏但保留滚轮翻页。
- PDF 放大预览比同尺寸 Markdown 预览额外增加 `8px` 垂直余量，避免适应宽度后的 A4 页面贴住视口底部而裁掉下侧灰边，同时减少底部留白；宽度和页面比例不变。
- PDF 视图解除文档后，Windows 文件句柄可能延迟释放；临时文件删除增加短间隔重试，切换格式、更新设置或关闭面板后均会清理旧预览。
- 离屏回归覆盖 36 条日程多页预览、第一页缩略图、跳转第 2 页、背景选项触发重生成、旧文件清理、防抖覆盖、运行中旧任务失效、后台串行和只发布最新结果；原范围弹窗、8 种字体、5 页超长卡片、空结果及文本提取回归继续通过。
