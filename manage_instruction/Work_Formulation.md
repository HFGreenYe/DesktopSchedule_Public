# Work Formulation

用途：记录当前阶段的总体规划、目标边界和小工单路线。由主窗口在审核决策窗口方案后维护。

---

# 当前状态：Markdown 真实落盘已完成

当前小工单已完成导出面板到既有 Markdown 写入 service 的接线，未扩展 PDF / PNG。

目标：

1. 已完成点击导出按钮时按当前选中的导出格式分发。
2. Markdown 已使用系统保存对话框选择路径，并自动保证 `.md` 后缀。
3. 已复用 `ScheduleExportService.write_markdown()` 和当前 `ExportOptions`，未在 UI 重复查询或拼装 Markdown。
4. 用户取消保存时不产生文件、不显示错误；成功和失败均提供明确反馈。

当前边界：

- 不修改数据库字段、日程 / 待办写入语义和导出 payload。
- 不实现 PDF / PNG 文件生成；选择未接入格式时只提示当前状态。
- 不加入默认目录持久化、最近导出记录、导出后自动打开文件等增强。
- 下一轮进入 PDF 中文字体、分页和真实文件生成方案审查；开始前重新建立阶段合同。
