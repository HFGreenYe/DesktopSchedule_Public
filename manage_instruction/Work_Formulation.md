# Work Formulation

用途：记录当前阶段的总体规划、目标边界和小工单路线。由主窗口在审核决策窗口方案后维护。

---

# 当前状态：导出功能下一轮待规划

最近阶段已经归档：

- 日 / 周 / 月 / 待办本地搜索与筛选。
- 卡片类列表排序。
- “全部日程”全局查询窗口。
- 导出 UI、统一 ExportPayload、Markdown 生成和真实预览。
- 待办看板、坐标看板、周界面与月界面的近期外观收尾。

下一阶段候选：

1. 接入保存路径并完成 Markdown 文件落盘。
2. 基于同一 ExportPayload 实现 PDF 中文字体与分页。
3. 实现 PNG 固定画布分页多图和预计图片数量提示。
4. 导出阶段完成后建立自动化测试与 CI 基线。

当前边界：

- 不修改数据库字段和日程 / 待办写入语义。
- PDF / PNG 必须复用统一导出 payload，不在 UI 类中重复查询和拼装数据。
- 开始下一轮源码修改前，先更新本文件、Work_Instruction.md 和 Work_Task_Prompts.md。
